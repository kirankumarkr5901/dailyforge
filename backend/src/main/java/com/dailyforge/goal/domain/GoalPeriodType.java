package com.dailyforge.goal.domain;

public enum GoalPeriodType {
    WEEK,
    MONTH,
    /** A calendar-year goal — "milestones for every month, yearly" (owner feedback). */
    YEAR,
    TARGET_DATE
}
