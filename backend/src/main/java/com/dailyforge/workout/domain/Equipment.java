package com.dailyforge.workout.domain;

/** Spec §6. Drives both the PR type-factor (§5.4) and the equipment chip colour (§9.2). */
public enum Equipment {
    DUMBBELL,
    BARBELL,
    BODYWEIGHT,
    MACHINE,
    NONE
}
