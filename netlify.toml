# Netlify build and headers for the DailyForge PWA.
#
# THIS FILE MUST STAY AT THE REPOSITORY ROOT. Netlify only looks for netlify.toml at the
# root of the repo; it does not go hunting inside subdirectories. Living in frontend/ —
# where it started, next to the app it configures — meant Netlify never read it at all,
# silently fell back to its defaults, and deployed the bare repo root: a build that
# "succeeded" in 7 seconds, uploaded 0 files, reported "Site is live", and 404'd on
# every request including the favicon. The file has to be where the tool looks, not
# where it reads most tidily.
#
# API_BASE_URL is set in Netlify's own environment (Site configuration > Environment
# variables) and baked into the bundle by frontend/scripts/set-api-url.mjs at build
# time, so pointing the app at a different backend never needs a code change.

[build]
  # Everything below is relative to this base, not to the repo root.
  base = "frontend"
  command = "npm run build:deploy"
  publish = "dist/frontend/browser"

[build.environment]
  NODE_VERSION = "22"

# Angular owns the routing; every path has to reach index.html or a hard refresh on
# /habits is a 404 from the CDN.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# ----------------------------------------------------------------- caching
#
# The rule that matters for an installed PWA: the files that decide *which build you
# are running* must never be served from cache, or a device keeps loading last week's
# app and never learns there is a new one. Everything content-hashed can be cached
# forever, because a new build gives it a new name.

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/ngsw.json"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/ngsw-worker.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ----------------------------------------------------------------- security

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=(), payment=()"
    # connect-src covers the API and Google's token endpoint; frame-src and script-src
    # are what Google Identity Services needs to render and run the sign-in button.
    # Tighten connect-src to the exact API origin once it is known (see docs/RUNBOOK.md).
    Content-Security-Policy = "default-src 'self'; script-src 'self' https://accounts.google.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://*.onrender.com https://accounts.google.com; frame-src https://accounts.google.com; worker-src 'self'; manifest-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'"
